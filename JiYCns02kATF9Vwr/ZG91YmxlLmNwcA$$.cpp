#include "bits/stdc++.h"
using namespace std;

double a[100];
pair<double, double> b[100];

int main()
{
    memset(a, 0, sizeof(a));
    memset(b, 0, sizeof(b));

    cout << a[0] << b[0].first << b[0].second << endl;
}